pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm run lint -- {staged_files}
    format:
      glob: "*.{js,jsx,ts,tsx,json,css}"
      run: pnpm run fmt -- {staged_files}

pre-push:
  commands:
    check:
      run: pnpm run check
